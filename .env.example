# My Memory - example environment variables
#
# This file is only used in development mode (when the app is NOT packaged).
# In dev, the Electron main process loads .env from the project root.
#
# Copy to .env and fill in the values you want to use.

# --- Gemini (default) ---
GEMINI_API_KEY=your_gemini_api_key_here

# --- OpenRouter (optional) ---
OPENROUTER_API_KEY=your_openrouter_api_key_here

# --- Ollama (local, optional) ---
# Used when you select the Ollama provider in the app.
# The app only allows localhost URLs for safety.
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1
